apiVersion: v1
kind: Namespace
metadata:
  name: kt-deepseek-harness
  labels:
    app.kubernetes.io/name: deepseek-harness
    kt.ktyun.cc/env: prod
    # Gateway API on Istio：为 Pod 注入 sidecar（permissive mTLS，
    # 不影响存量集群内调用）
    istio-injection: enabled
