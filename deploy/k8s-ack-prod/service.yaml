apiVersion: v1
kind: Service
metadata:
  name: deepseek-harness
  labels:
    app.kubernetes.io/name: deepseek-harness
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deepseek-harness
  ports:
    - name: http
      port: 80
      targetPort: 8080
